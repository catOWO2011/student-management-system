server:
  # Add /api as a prefix for every request in the server properties
  servlet:
    context-path: /api
  # Set the default port from 8080 to 5000
  port: 5000

app:
  # DataSource Configuration
  datasource:
    jdbc-url: jdbc:postgresql://ep-delicate-star-454119.us-east-2.aws.neon.tech/neondb
    username: nanocatowo2011
    password: 3zFwvISLM7br
    pool-size: 30